# Track: s1-core

Mission:

- scale the locked `s0-core` recipe to `~600M`

Entry condition:

- `s0-core` must be promoted and frozen first

Allowed changes:

- capacity scaling
- wider visual and decoder budgets
- stronger adapters

Forbidden changes until explicitly approved:

- changing target representation
- changing benchmark protocol
- redefining the main product contract
