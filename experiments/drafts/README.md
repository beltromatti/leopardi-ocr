# Experiment Drafts

This directory stores lightweight draft experiment specs before they become registered entries.

Use it for:

- first-pass experiment ideas
- run stubs created by `scripts/create_experiment.py`
- temporary notes before promotion into `experiments/registry/`
