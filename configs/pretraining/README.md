# Pretraining Configs

Use one file per training stage.

Recommended naming:

- `s0_p0_*`
- `s0_p1_*`
- `s0_p2_*`
- `s0_p3_*`

Keep generic defaults lightweight and push stable decisions into stage-specific files.
