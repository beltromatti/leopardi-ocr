# Inference Configs

These configs define serving and decode policies for promoted Leopardi artifacts.

They should pin:

- the promoted artifact variant
- the primary and fallback runtime
- mode budgets for `fast`, `standard`, and `hard`
- routing thresholds
- validation and assembly policy
