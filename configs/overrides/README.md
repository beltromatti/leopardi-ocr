# Overrides

This directory is for short-lived ablation overrides only.

Rules:

- never store the only copy of an important experiment setting here
- never promote an experiment whose behavior depends on an undocumented override
- once an override becomes standard, move it into the main layered config stack
