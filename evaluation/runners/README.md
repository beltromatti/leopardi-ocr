# Runners

Runners should support:

- local validation on a small shard
- full benchmark sweeps
- latency-only smoke tests
- generation of leaderboard-ready reports

Runner outputs should be keyed by:

- experiment id
- benchmark protocol version
- hardware tag
- decode mode
