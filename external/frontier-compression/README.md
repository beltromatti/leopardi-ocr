# Frontier Compression Submodules

- `torchao`: PyTorch-native quantization, QAT, sparsity, and low-precision training
- `llm-compressor`: deployment-oriented quantization stack aligned with `vLLM`

These repos matter because compression is no longer a late deployment step. It feeds back into architecture and training choices.
