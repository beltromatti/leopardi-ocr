# Frontier Runtime Submodules

- `vllm`: primary open serving baseline for high-throughput LLM and VLM inference
- `sglang`: aggressive modern serving stack with strong structured-output and frontier-model support
- `flashinfer`: kernel engine for attention, GEMM, MoE, and mixed serving workloads

These repos matter because Leopardi must compete on both accuracy and parsing speed.
