# Frontier Structured Decoding Submodules

- `xgrammar`: high-adoption grammar engine integrated into major serving stacks
- `llguidance`: fast constrained-decoding engine with strong benchmark posture on JSON-schema-style workloads

These repos matter because Leopardi must emit exact Markdown plus LaTeX, not merely plausible text.
