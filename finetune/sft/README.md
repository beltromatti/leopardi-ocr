# SFT

Date locked: 2026-04-08

SFT data should emphasize:

- exact Markdown block boundaries
- correct reading order
- math fidelity
- robust handwriting decoding
- hard tables and figures

Operational rule:

- `F0` is for clean exactness
- `F1` is for specialist long-tail slices
- `F2` is for local repair

Do not mix those objectives into one anonymous SFT pool.
